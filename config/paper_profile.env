# Version-controlled paper risk profile.
# This file is safe to keep in Git because it contains no secrets.
# Current strategy decision: keep paper leverage conservative at 1.2x while
# validating expectancy. Promote to 1.5x only after a large enough positive
# sample, otherwise keep the contour capped.

SCALPER_PAPER_INITIAL_CASH_RUB=300000
SCALPER_PAPER_MAX_GROSS_LEVERAGE=1.2
